--- a/kernel2minor.c
+++ b/kernel2minor.c
@@ -284,7 +284,7 @@
     perror("Can't open kernel file");
     exit(-1);
   }
-  r = creat(res_file, 0);
+  r = creat(res_file, S_IRWXU);
   if(r <= 0){
     perror("Can't create result file");
     close(k);
